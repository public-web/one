includes:
    - vendor/larastan/larastan/extension.neon

parameters:
    level: 5
    paths:
        - app
        - config
        - database
        - routes

    # Exclude problematic paths
    excludePaths:
        - bootstrap
        - storage
        - vendor
